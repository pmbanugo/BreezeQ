# BreezeQ Dashboard Configuration

# Broker Configuration
FRONTEND_PORT=5555
BACKEND_PORT=5556
WEB_PORT=3000
WEB_HOST=localhost

# Worker Configuration
WORKER_COUNT=3
JOB_TYPE=math
BROKER_HOST=localhost

# Client Configuration
SUBMISSION_INTERVAL_MS=2000
MAX_JOBS=100
JOB_TYPES=math,data-processing,image-resize

# Process Management
AUTO_RESTART=true
RESTART_DELAY=5000
